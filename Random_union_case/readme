this folder contains test cases. 

for test case:
The first line tells # of node, and each line represents: #1 connect w/ #2

file name: node_num + iterate_num + #

use RandomFileGenerator.py to generate more grpah if needed
